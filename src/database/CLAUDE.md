# Database Guidance

- Inspect the schema with Laravel Boost's `database-schema` before writing migrations or changing models.
- Use factories when creating models in tests.
- Follow existing migration, foreign-key, index, and model conventions in sibling files.
