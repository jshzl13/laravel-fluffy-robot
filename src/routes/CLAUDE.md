# Routing Guidance

- Inspect routes with `php artisan route:list` before changing route behavior. Use its filters to narrow the output when helpful.
- Follow the application's established conventions for route naming, route-model binding, middleware, validation, and API responses.
- For new APIs, use Eloquent API Resources and versioning only when existing API routes do the same.

